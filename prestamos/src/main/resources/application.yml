server:
  port: 8088

spring:
  application:
    name: ms-prestamos

  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka