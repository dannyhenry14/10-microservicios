server:
  port: 8089

spring:
  application:
    name: ms-usuarios

  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka